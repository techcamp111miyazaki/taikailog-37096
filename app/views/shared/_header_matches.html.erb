<header>
      <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
          <div class="row">
            <div class="col-sm-8 col-md-7 py-4">
              <h4 class="text-white">About</h4>
              <p class="text-muted">これは大会の記録を遠隔で管理できるアプリケーションです。<br>全国各地で開催される大会記録をいつでもどこからでも確認できます。<br>操作はシンプルで誰でも閲覧可能です。</p>
            </div>
            <div class="col-sm-4 offset-md-1 py-4">
            <% if user_signed_in? %>
              <h4 class="text-white">Log out</h4>
              <ul class="list-unstyled">
                <li><%= link_to "#{current_user.name}", root_path, class: "text-white text-decoration-none" %></li>
                <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-white text-decoration-none" %></li>
              </ul>
            <% else %>
              <h4 class="text-white">Sign in</h4>
              <ul class="list-unstyled">
                <li><%= link_to "新規登録", new_user_registration_path, class: "text-white text-decoration-none" %></li>
                <li><%= link_to "ログイン", new_user_session_path, class: "text-white text-decoration-none" %></li>
              </ul>
            <% end %>
            </div>
          </div>
        </div>
      </div>
      <div class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container">
          <a href="/" class="navbar-brand d-flex align-items-center">
            <strong>大会Log</strong>
          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
      </div>

      <nav class="navbar navbar-expand-md navbar-dark bg-dark" aria-label="Fourth navbar example">
        <div class="container-fluid">
          <%# <a class="navbar-brand" href="#"><%= @event.event_title %>
          <%= link_to "#{@event.event_title}", event_matches_path(@event.id) ,class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarsExample04">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="dropdown04" data-bs-toggle="dropdown" aria-expanded="false">試合記録</a>
                <ul class="dropdown-menu" aria-labelledby="dropdown04">
                  <% @matches_turn.each do |turn_name| %>
                    <li><a class="dropdown-item" href="#"><%= turn_name.turn.name %></a></li>
                  <% end %>
                </ul>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true" id="current-list">---</a>
              </li>
            </ul>
            <%= search_form_for @matches.ransack(params[:q]), url: search_event_matches_path(@event), html: {class: "d-flex"} do |f| %>
            <%= f.text_field :player_name_1_or_player_name_2, placeholder: "Search", class: 'form-control me-2' %>
            <%= f.label :submit do %>
              <input type="submit" value="Search" class="btn btn-outline-success">
              <%= f.submit :submit, id: "q_submit", style: "display: none;"%>
            <% end %>
          <% end %>
          </div>
        </div>
      </nav>
    </header>